#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Display alphabet
 *
 * Return: Always 0 for (success)
*/

int main(void)
{
	for (int i = 97; i < 123; i++)
		putchar(i);
	putchar('\n');
	return (0);
}
